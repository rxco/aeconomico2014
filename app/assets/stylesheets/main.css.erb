/*! HTML5 Boilerplate v4.3.0 | MIT License | http://h5bp.com/ */
html,
button,
input,
select,
textarea {
    color: #222;
}

html {
    font-size: 1em;
    line-height: 1.4;
}

::-moz-selection {
    background: #b3d4fc;
    text-shadow: none;
}

::selection {
    background: #b3d4fc;
    text-shadow: none;
}

hr {
    display: block;
    height: 1px;
    border: 0;
    border-top: 1px solid #ccc;
    margin: 1em 0;
    padding: 0;
}

audio,
canvas,
img,
video {
    vertical-align: middle;
}

fieldset {
    border: 0;
    margin: 0;
    padding: 0;
}

textarea {
    resize: vertical;
}

.browsehappy {
    margin: 0.2em 0;
    background: #ccc;
    color: #000;
    padding: 0.2em 0;
}


/* ==========================================================================
   Applicattio custom styles
   ========================================================================== */
/*====== index ======*/
.intro-message {
    color: #fff;
    position: relative;
}
.intro-message-m {
    color: #292929;
    position: relative;
}

.pos-fond{
   padding: 10px;
   margin: 10px 0;
   margin: 6% 0 0 0;
}

.intro-fondo{
   background: rgba(0,0,0, 0.5);
   height: 100%;
   /*border-radius: 10px;*/
}
.intro-fondo-m{
   background: rgba(255,255,255, 0.4);
   /*border-radius: 10px;*/
}
.intro-titulo{
    font-size: 40px;
    font-family: 'Gotham-Bold',Arial;
}
.aller{
    font-family: 'Aller-Light', sans-serif;
}
.abold{
    font-family: 'Aller-Bold', sans-serif;
}
.btn-transparenre{
    background: transparent;
    border: 1px solid #fff;
    color: #fff;
    cursor: pointer;
    font-family: 'Aller-Light', sans-serif;
    font-style: normal;
    -webkit-font-smoothing: antialiased;
    text-align: center;
    text-decoration: none;
    text-transform: uppercase;
    margin: 0 auto;
    padding: 10px 7px;
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
}

.btn-transparenre:hover{
    background: rgba(255,255,255, 1);
    color: #292929 !important;
}
.btn-transparenre:visited{
    color: #fff;
}

.btn-transparenre-m{
    background: transparent;
    border: 1px solid #292929;
    color: #292929;
    cursor: pointer;
    font-family: 'Aller-Light', sans-serif;
    font-style: normal;
    -webkit-font-smoothing: antialiased;
    text-align: center;
    text-decoration: none;
    text-transform: uppercase;
    margin: 0 auto;
    padding: 10px 7px;
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
}

.btn-transparenre-m:hover{
    border: 1px solid #292929;
    background: rgba(255,255,255, 0.8);
    color: #292929;
}
.btn-transparenre-m:visited{
    color: #292929;
}

._color{
        color: #d23138 ;
}
#footer {
    position:fixed;
    left:0px;
    bottom:0px;
    height:90px;
    width:100%;
    z-index: 3;
}
/* IE 6 */
* html #footer {
   position:absolute;
   top:expression((0-(footer.offsetHeight)+(document.documentElement.clientHeight ? document.documentElement.clientHeight : document.body.clientHeight)+(ignoreMe = document.documentElement.scrollTop ? document.documentElement.scrollTop : document.body.scrollTop))+'px');
}

.fondo-body{
    /*background-image: url(../img/home-pattern.png), url(../img/landing-background.jpg);
    background-repeat: repeat, no-repeat;
    background-position: center center, center center;
    background-attachment: fixed;
    background-size: auto, cover;*/
    background-image: url(<%= asset_data_uri'back-index.png'%>);
    background-repeat:no-repeat;
    background-attachment:fixed;
    background-position:center;
    background-size: cover;
    background-color:rgba(255,255,255, 0.3);
}

/* For mobile devices */
@media only screen and (max-width: 992px) {
  .fondo-body {
    /* The file size of this background image is 93% smaller
     * to improve page load speed on mobile internet connections */
    background-image: url(<%= asset_data_uri'back-index-desktops.png'%>);
  }
}

/* === Menu === */
.dropdown-menu li:not(:first-child){
    box-shadow: inset 0 1px #c6d0da;
}
.dropdown-menu li{
    height: 60px;
    line-height: 60px;
}
.fondo-logo-index{
    top: 60px;
    background: #FFF;
    height:30px;
    position: relative;
}
.logo-index{
    position: relative;
    bottom: 12px;
}
.bordr-img{
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
}
/* === Box Result === */
.box-result{
    background: #fff;
    font-family: 'Aller-Light', sans-serif;
    min-height: 85px;
    max-height: 85px;
    margin: 2px -10px 10px -10px;
    /*-webkit-box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
    box-shadow: 0 2px 10px rgba(0, 0, 0, .175);*/
    border: 1px solid;
    border-color: #e5e6e9 #dfe0e4 #d0d1d5;
    -webkit-border-radius: 3px;
    -moz-border-radius: 3px;
    border-radius: 3px;
}
.box-result:hover{
    -webkit-box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
    -moz-box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
    box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
}
.clogo-box{
    float: left;
    min-width: 64px;
    max-width: 64px;
    min-height: 88px;
}
.box-icon{
    display: inline-block;
    width: 60px;
    text-align: center;
    text-transform: none;
    font-weight: normal;
    font-style: normal;
    font-variant: normal;
    font-family: 'Ionicons';
    line-height: 2;
    speak: none;
    -webkit-font-smoothing: antialiased;
    font-size: 40px;
    color: #fff;
}
.box-icon-ir{
    display: inline-block;
    width: 64px;
    text-align: center;
    text-transform: none;
    font-weight: normal;
    font-style: normal;
    font-variant: normal;
    font-family: 'Ionicons';
    line-height: 1;
    speak: none;
    -webkit-font-smoothing: antialiased;
    font-size: 34px;
    color: #fff;
    margin-top:14px; 
}
.box-ir{
    background: #d3d3d3;
}
.text-ir{
    color: #fff;
    font-size: 14px;
    text-align: center;
    float: left;
    width: 100%;
    text-decoration: none;
}

.regresar-result{
    background:#d3d3d3 ;
    color: #FFF !important;
}

/* ===  Efectt  === */

.isotope-item {
    z-index: 2;
}
.isotope-hidden.isotope-item {
    pointer-events: none;
    z-index: -1;
}
.isotope,
.isotope .isotope-item {
  /* change duration value to whatever you like */

    -webkit-transition-duration: 0.8s;
    -moz-transition-duration: 0.8s;
    transition-duration: 0.8s;
}
.isotope {
    -webkit-transition-property: height, width;
    -moz-transition-property: height, width;
    transition-property: height, width;
}
.isotope .isotope-item {
    -webkit-transition-property: -webkit-transform, opacity;
    -moz-transition-property: -moz-transform, opacity;
    transition-property: transform, opacity;
}

.logo-visible{
    position: fixed;
    right: 0;
    top: 70%;
    z-index: 1;
}
.sombra-img{
    background: #FFF;
    border-radius: 10px 0 0 10px;
    -webkit-box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
    box-shadow: 0 2px 10px rgba(0, 0, 0, .175);
}

.btn-oyo{
    color:#fff;
    margin: -1px -1px 0 8px;
    font-size: 26px;
    width: 40px;
    height: 45px;
    line-height: 1.7;
    text-align: center;
    background: #d23138;
}
.btn-oyo:hover{
    color:#dadada;
}

/* ==========================================================================
   Media Queries
   ========================================================================== */

@media only screen and (min-width: 35em) {

}

@media print,
       (-o-min-device-pixel-ratio: 5/4),
       (-webkit-min-device-pixel-ratio: 1.25),
       (min-resolution: 120dpi) {

}

/* ==========================================================================
   Helper classes
   ========================================================================== */

.ir {
    background-color: transparent;
    border: 0;
    overflow: hidden;
    *text-indent: -9999px;
}

.ir:before {
    content: "";
    display: block;
    width: 0;
    height: 150%;
}

.hidden {
    display: none !important;
    visibility: hidden;
}

.visuallyhidden {
    border: 0;
    clip: rect(0 0 0 0);
    height: 1px;
    margin: -1px;
    overflow: hidden;
    padding: 0;
    position: absolute;
    width: 1px;
}

.visuallyhidden.focusable:active,
.visuallyhidden.focusable:focus {
    clip: auto;
    height: auto;
    margin: 0;
    overflow: visible;
    position: static;
    width: auto;
}

.invisible {
    visibility: hidden;
}

.clearfix:before,
.clearfix:after {
    content: " ";
    display: table;
}

.clearfix:after {
    clear: both;
}

.clearfix {
    *zoom: 1;
}

/* ==========================================================================
   Print styles
   ========================================================================== */

@media print {
    * {
        background: transparent !important;
        color: #000 !important;
        box-shadow: none !important;
        text-shadow: none !important;
    }

    a,
    a:visited {
        text-decoration: underline;
    }

    a[href]:after {
        content: " (" attr(href) ")";
    }

    abbr[title]:after {
        content: " (" attr(title) ")";
    }

    .ir a:after,
    a[href^="javascript:"]:after,
    a[href^="#"]:after {
        content: "";
    }

    pre,
    blockquote {
        border: 1px solid #999;
        page-break-inside: avoid;
    }

    thead {
        display: table-header-group;
    }

    tr,
    img {
        page-break-inside: avoid;
    }

    img {
        max-width: 100% !important;
    }

    @page {
        margin: 0.5cm;
    }

    p,
    h2,
    h3 {
        orphans: 3;
        widows: 3;
    }

    h2,
    h3 {
        page-break-after: avoid;
    }
}